# disable all the extra services
# /etc/init.d/bcm-device-nodes on
# /etc/init.d/bcmubusbridge on
# /etc/init.d/boot on
# /etc/init.d/brcm_kthrd_prios on
# /etc/init.d/cgconfig not sure
# /etc/init.d/cgred not sure
# /etc/init.d/conntrackd not sure
# /etc/init.d/cron on
/etc/init.d/cwmpd disable
/etc/init.d/cwmpdboot disable
# /etc/init.d/ddns not sure
# /etc/init.d/dhcpopassthrud not sure
# /etc/init.d/dnsmasq on
# /etc/init.d/done on
/etc/init.d/dropbear disable
# /etc/init.d/ethernet not sure
# /etc/init.d/firewall undecided
/etc/init.d/hostmanager disable
# /etc/init.d/igmpproxy not sure
# /etc/init.d/intercept not sure
# /etc/init.d/ipset not sure
# /etc/init.d/iqos not sure
# /etc/init.d/led not sure
# /etc/init.d/ledfw not sure
# /etc/init.d/log not sure
/etc/init.d/lte-doctor-logger disable
# /etc/init.d/mcsnooper not sure
# /etc/init.d/minidlna not sure
# /etc/init.d/minidlna-procd disable
# /etc/init.d/miniupnpd not sure
# /etc/init.d/mmdbd not sure
/etc/init.d/mmpbxd disable
/etc/init.d/mobiled disable
# /etc/init.d/mountd not sure
# /etc/init.d/mwan not sure
/etc/init.d/neighm disable
# /etc/init.d/network on
# /etc/init.d/nginx on
# /etc/init.d/odhcpd not sure
# /etc/init.d/phypower not sure
# /etc/init.d/platform-gpl not sure
# /etc/init.d/portmirror not sure
# /etc/init.d/power not sure
/etc/init.d/pppoe-relay disable
/etc/init.d/pppoe-relay-tch disable
# /etc/init.d/pre-mwan not sure
/etc/init.d/qos disable
# /etc/init.d/ra not sure
/etc/init.d/random_seed disable
# /etc/init.d/restore not sure
/etc/init.d/samba disable
/etc/init.d/samba-nmbd disable
# /etc/init.d/sscert not sure
# /etc/init.d/sudo on
# /etc/init.d/swmdk not sure
# /etc/init.d/sysctl on
# /etc/init.d/sysctl-tch on
# /etc/init.d/sysfixtime on
# /etc/init.d/syslog_fwd on
# /etc/init.d/sysntpd on
# /etc/init.d/system on
# /etc/init.d/telnet not sure
# /etc/init.d/tod on
/etc/init.d/transformer disable
/etc/init.d/umount disable
# /etc/init.d/usbmode on
/etc/init.d/wansensing disable
# /etc/init.d/warmboot on
/etc/init.d/watchdog-tch disable
# /etc/init.d/wol on
/etc/init.d/xdsl disable
# /etc/init.d/xtm on